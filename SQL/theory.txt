activity
- create
1. insert
2. select
3. update
4. delete

create table student{
    id int primary key , age int
}

INSERT INTO student (id, age) VALUES (1, 20);

SELECT id, age FROM student WHERE age > 18;

UPDATE student SET age = 21 WHERE id = 1;

DELETE FROM student WHERE id = 1;

connecting node.js to sql database we use node.js backend to connect database

write a syntax for get,post,delete,update in express

get
app.get('/students', (req, res) => {
  res.send('Get list of students');
});

post
app.post('/students', (req, res) => {
  const student = req.body;
  res.send(`Create student: ${JSON.stringify(student)}`);
});

update
app.put('/students/:id', (req, res) => {
  const id = req.params.id;
  const updates = req.body;
  res.send(`Update student ${id} with ${JSON.stringify(updates)}`);
});


activity
rebrushing sql

